Scene
=====
The `Scene` object provides the central abstraction of the `meshrender` package.
It manages objects, lights, and the camera, and it wraps the key functions that
render images.

Scene
~~~~~
.. autoclass:: meshrender.Scene
